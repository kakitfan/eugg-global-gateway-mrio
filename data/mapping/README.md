# Local Mapping Data

Place `mapping regions_WBhh_GLORIA_WBpop_EDGAR.xlsx` here if you need to
regenerate `country_population_weights_wb_2023.csv` with
`build_country_population_weights.py`.

This directory is intentionally ignored by Git except for this README.
